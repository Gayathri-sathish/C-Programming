/* leap year*/
#include<stdio.h>
int main()
{
	int age;
	printf(" enter any age:");
	scanf("%d",&age);
	if((age>26) && (age<35))
	{
		printf(" %d udder limitte ",age);
	}
	else
	{
	printf(" %d not udder limitte",age);
    }
}
